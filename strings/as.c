#include<stdio.h>

#include<string.h>
int main()
{
    int i;
    char s[100],s2[100];
    printf("Enter a string: ");
    fgets(s, sizeof(s), stdin);
    printf("Enter a string: ");
    fgets(s, sizeof(s2), stdin);
   /*/ if(strlen(s)==strlen(s2))
    print("yes");
    else
    print("no");*/
    return(0);
}   